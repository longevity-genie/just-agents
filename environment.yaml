name: just-agents
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - loguru
  - requests
  - pytest #for tests
  - typer #for CLI
  - twine>=5.1.1 #for publishing
  - pip:
      - litellm>=1.41.28
      - numpydoc
      - semanticscholar>=0.8.4
      - Mako>=1.3.5